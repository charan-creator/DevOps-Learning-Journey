- name: variables
  hosts: localhost
  vars:
    Course: "ansible"
    Trainer: "Nagacharan"

  tasks:
  - name: print information
    ansible.builtin.debug:
      msg: "Hi, we are learning {{Course}} from {{Trainer}}"

