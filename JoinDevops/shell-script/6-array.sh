#!/bin/bash

devopstools=("linux" "git" "jenkins" "python" )

#index always starts with 0
#size of the array is 4
echo "first tool to learn in the list is ${devopstools[0]}"
echo "tools in devops are ${devopstools[@]}"
